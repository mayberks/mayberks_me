import 'package:flutter/widgets.dart';

/// the 2 height sizedbox.
const heightTwo = SizedBox(
  height: 2,
);

/// the 3 height sizedbox.
const heightTree = SizedBox(
  height: 3,
);

/// the 5 height sizedbox.
const heightFour = SizedBox(
  height: 5,
);

/// the 10 height sizedbox.
const heightTen = SizedBox(
  height: 10,
);

/// the 15 height sizedbox.
const heightFifteen = SizedBox(
  height: 15,
);

/// the 17 height sizedbox.
const heightSeventeen = SizedBox(
  height: 17,
);

/// the 20 height sizedbox.
const heightTwenty = SizedBox(
  height: 20,
);

/// the 25 height sizedbox.
const heightTwentyFive = SizedBox(
  height: 25,
);

/// the 30 height sizedbox.
const heightThirty = SizedBox(
  height: 30,
);

/// the 40 height sizedbox.
const heightForty = SizedBox(
  height: 40,
);

/// the 50 height sizedbox.
const heightFifty = SizedBox(
  height: 50,
);

/// the 5 width sizedbox.
const widthFour = SizedBox(
  width: 5,
);

/// the 10 width sizedbox.
const widthTen = SizedBox(
  width: 10,
);

/// the 15 width sizedbox.
const widthFifteen = SizedBox(
  width: 15,
);

/// the 17 width sizedbox.
const widthSeventeen = SizedBox(
  width: 17,
);

/// the 20 width sizedbox.
const widthTwenty = SizedBox(
  width: 20,
);

/// the 25 width sizedbox.
const widthTwentyFive = SizedBox(
  width: 25,
);

/// the 30 width sizedbox.
const widthThirty = SizedBox(
  width: 30,
);
